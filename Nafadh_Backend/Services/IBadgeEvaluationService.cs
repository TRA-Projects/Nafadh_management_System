// <auto-generated>
// Added as part of Nafadh backend upgrade (Phase 2 - Contract Alignment).
// Domain-owning teams may extend business logic in Services; Models/DbContext define the schema contract.
// </auto-generated>

namespace Nafadh_Backend.Services
{
    /// <summary>
    /// Automatic badge-granting engine — checks every fixed badge's condition
    /// against a trainee's current data and grants any newly-earned badges.
    /// Never manually triggered by a client; called from the mutation points
    /// that could cause a badge condition to newly become true (an evaluation
    /// being scored, a submission being graded, etc.) per finalized decision #13.
    /// </summary>
    public interface IBadgeEvaluationService
    {
        // Re-checks every badge condition for the trainee behind this enrollment.
        Task EvaluateEnrollmentEvaluationAsync(int enrollmentId);

        // Re-checks every badge condition for a trainee directly.
        Task EvaluateTraineeAsync(int traineeId);
    }
}
