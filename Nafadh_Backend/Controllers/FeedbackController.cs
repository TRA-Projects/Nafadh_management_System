// <auto-generated>
// Added as part of Nafadh backend upgrade (Phase 2 - Contract Alignment).
// Domain-owning teams may extend business logic in Services; Models/DbContext define the schema contract.
// </auto-generated>

using Microsoft.AspNetCore.Mvc;
using Nafadh_Backend.DTOs;
using Nafadh_Backend.Enums;
using Nafadh_Backend.Services;

namespace Nafadh_Backend.Controllers
{
    [ApiController]
    [Route("api/[controller]")]
    public class FeedbackController : ControllerBase
    {
        private readonly IFeedbackService _service;

        public FeedbackController(IFeedbackService service)
        {
            _service = service;
        }

        // GET: api/Feedback/criteria?appliesTo=TrainerRating
        [HttpGet("criteria")]
        public async Task<IActionResult> GetCriteria([FromQuery] NFD_FeedbackType? appliesTo)
        {
            var criteria = await _service.GetCriteriaAsync(appliesTo);
            return Ok(criteria);
        }

        // POST: api/Feedback
        [HttpPost]
        public async Task<IActionResult> Create([FromBody] CreateFeedbackDTO dto)
        {
            if (!ModelState.IsValid) return BadRequest(ModelState);

            var created = await _service.CreateFeedbackAsync(dto);
            return Ok(created);
        }

        // GET: api/Feedback/trainer/{trainerId}
        [HttpGet("trainer/{trainerId}")]
        public async Task<IActionResult> GetTrainerSummary(int trainerId)
        {
            var summary = await _service.GetTrainerSummaryAsync(trainerId);
            return Ok(summary);
        }

        // GET: api/Feedback/batch/{batchId}
        [HttpGet("batch/{batchId}")]
        public async Task<IActionResult> GetBatchSummary(int batchId)
        {
            var summary = await _service.GetBatchSummaryAsync(batchId);
            return Ok(summary);
        }

        // GET: api/Feedback/module/{moduleId}/trainee/{traineeId}/pending
        [HttpGet("module/{moduleId}/trainee/{traineeId}/pending")]
        public async Task<IActionResult> GetPending(int moduleId, int traineeId)
        {
            var pending = await _service.GetPendingAsync(moduleId, traineeId);
            return Ok(pending);
        }
    }
}
